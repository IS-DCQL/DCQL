(: BaseX is schema-less: the PESR_HNS_Protocol entity needs no schema; its nested
   composition / process / microstructure / performance fields appear on first insert. :)
db:create("pesr_hns_protocol")
